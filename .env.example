PORT=3000
DATA_DIR=./data
NODE_ENV=development
# Set to the exact HTTPS origin on Render, e.g. https://your-service.onrender.com
APP_ORIGIN=
# Render generates this automatically. Keep it stable to restore browser backups.
# Local development creates a private key in DATA_DIR when this is unset.
BACKUP_SECRET=
